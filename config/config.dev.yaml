general:
  mode: dev

# Reduced parameters for dev – fast pipeline testing
features:
  window_sec: 15          # 15s window instead of 30s
  stride_sec: 3           # 3s stride instead of 5s
  min_keyboard_events: 3  # less strict threshold
  min_mouse_events: 3

model:
  seq_len: 8     # shorter sequence (was 24)
  epochs: 10     # fewer epochs (was 25)
