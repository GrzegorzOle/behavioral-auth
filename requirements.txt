# CPU-only PyTorch. The daemon scores on CPU/ONNX and trains on CPU
# (model.device defaults to cpu); the default +cu build drags in ~2.7 GB of
# unused nvidia CUDA libraries, which is the whole self-contained-bundle bloat.
--extra-index-url https://download.pytorch.org/whl/cpu
duckdb==1.1.3
# Input capture is platform-split; pip installs whichever matches the OS.
evdev==1.7.0 ; sys_platform == "linux"     # Linux: reads /dev/input
pynput==1.7.6 ; sys_platform == "win32"    # Windows: global keyboard/mouse hook
loguru==0.7.3
numpy==2.0.1
onnx==1.16.2
onnxruntime==1.19.2
opencv-contrib-python>=4.10,<5
pandas==2.2.2
pydantic==2.8.2
pyyaml==6.0.1
torch==2.4.0+cpu
